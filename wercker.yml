box: python:3.6

build:
  steps:
    - script:
        name: Install Dependencies
        code: |
          sudo pip install -r test/requirements-testing.txt
          sudo pip install .
    - script:
        name: Run Tests
        code: pytest -x